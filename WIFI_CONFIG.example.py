# Copy to WIFI_CONFIG.py on the device and fill in. Real secrets live in
# edge-net-secrets, never in git. WIFI_CONFIG.py is gitignored.
SSID = "Pirie"
PSK = "CHANGE_ME"          # edge-net AP passphrase (see edge-net-secrets)
COUNTRY = "GB"
